# Generated by `node scripts/fetch-parcels.mjs` from the live Rock Island County
# ArcGIS FeatureServer. rock-island-parcels.json is deliberately written without
# indentation because it ships to the browser as a static asset and its size matters.
# Prettier would re-indent it and multiply the transfer size.
public/data/

# The assignment specification, owned upstream by prismteam-ai. Reformatting it would
# insert blank lines after headings and shift every line number — and the plan and the
# QA citations reference README.md lines 14/15/18/29/32/33 as the graded criteria.
README.md

# Verbatim copies of maplibre-gl@6.3.0 dist files, written by
# `scripts/copy-maplibre-worker.mjs`. Minified upstream code — reformatting it would only
# make the diff-free copy look like our own source.
public/maplibre/
